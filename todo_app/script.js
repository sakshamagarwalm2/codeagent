let todoList = []; const todoInput = document.getElementById("todo-input"); const addTodoBtn = document.getElementById("add-todo"); const todoListElement = document.getElementById("todo-list"); addTodoBtn.addEventListener("click", addTodo); function addTodo() { const newTodo = todoInput.value.trim(); if (newTodo) { todoList.push({ text: newTodo, completed: false }); todoInput.value = ""; displayTodoList(); } } function displayTodoList() { todoListElement.innerHTML = ""; todoList.forEach((todo, index) => { const todoElement = document.createElement("li"); todoElement.textContent = todo.text; todoElement.addEventListener("click", () => { todo.completed = !todo.completed; displayTodoList(); }); if (todo.completed) { todoElement.style.textDecoration = "line-through"; } todoListElement.appendChild(todoElement); }); }
